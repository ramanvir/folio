# OS
.DS_Store
.AppleDouble
Thumbs.db
desktop.ini

# Editors
.idea/
.vscode/
*.swp
*~

# Agent / tooling local state (kept out of the repo even where a global
# ignore isn't set up — this one holds machine-specific permissions)
.claude/settings.local.json

# Tooling / dependencies (none are needed to run the app)
node_modules/
package-lock.json
.playwright-cli/
*.log

# Scratch
tmp/
*.tmp
